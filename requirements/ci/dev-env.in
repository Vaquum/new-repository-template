# Dev-env set: mirrors [project.optional-dependencies].dev in
# pyproject.toml. The contract tests assert the ruff and pyright pins
# here match governance.yml, so the three sources cannot drift.
pytest
ruff==0.15.11
pyright==1.1.408
vulture>=2.14
coverage[toml]>=7.6
pyyaml>=6.0
pip-audit>=2.7
