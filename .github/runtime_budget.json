{
  "schema_version": 1,
  "max_total_seconds": 120,
  "slowest_tests_limit": 10,
  "baseline": {
    "note": "Scaffolding ceiling, not a measured one. The template's own suite is a single import test and runs in well under a second, so there is no observed spread to derive a ceiling from. A derived repository recalibrates this against its own observed maximum plus headroom, and replaces this note with the observation window.",
    "workflow": "pr_checks_tests",
    "job": "pr_checks_tests",
    "sample_count": 0,
    "observed_max_seconds": null,
    "headroom_seconds": null
  }
}
